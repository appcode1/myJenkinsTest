apiVersion: v1
kind: Pod
metadata:
 name: webapi-yml
 labels:
  app: webapi-yml
spec:
 containers:
  - image: 192.168.8.164/myharborproject1/webapitest:latest
    imagePullPolicy: IfNotPresent
    name: webapitest-container
    ports:
     - containerPort: 8080

